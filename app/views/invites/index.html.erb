<%= link_to "All Users", users_path %>
<%= link_to "All Events", events_path %>

<h1> View All Invites </h1>

<% @invites.each do |invite| %>
    <p><%= link_to invite.title, invite_path(invite) %></p>
<% end %>
